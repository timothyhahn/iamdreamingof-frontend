import type { PageLoad } from '../../.svelte-kit/types/src/routes/$types';

/**
export const load: PageLoad = async () => {
	const result = await fetch('https://cdn.iamdreamingof.com/today.json', {mode: "no-cors"});
	return {
		today: await result.json()
	};
}
 **/